{"id":455454047,"lang":"cpp","time":"1 year, 4 months","timestamp":1613204538,"status_display":"Runtime Error","runtime":"N/A","url":"/submissions/detail/455454047/","is_pending":"Not Pending","title":"Course Schedule II","memory":"N/A","code":"class Solution {\npublic:\n    int time=0;\n    vector<vector<int>> dag;\n    vector<bool> vis;\n    struct node{\n        int ver,fin;  \n    };\n    vector<node> so;\n    static bool cmp(node a,node b){\n        return a.fin>b.fin;\n    }\n    void dfs(int x){\n        time++;\n        for(auto i : dag[x]){\n            if(vis[i]==false){\n                dfs(i);\n                vis[i]=true;\n            }\n        }\n        time++;\n        so[x].ver=x;\n        so[x].fin=time;\n    }\n    vector<int> findOrder(int num, vector<vector<int>>& pre) {\n        vector<int> ans;\n        if(pre.size()==0){\n            for(int i=0;i<num;i++) ans.emplace_back(i);\n            return ans;\n        }\n        dag.resize(num);\n        so.resize(num);\n        vis=vector<bool>(num,false);\n        int m=pre.size();\n        for(int i=0;i<m;i++){\n            dag[pre[i][1]].emplace_back(pre[i][0]);\n        }\n        for(int i=0;i<num;i++)\n            if(vis[i]==false)\n                dfs(i);\n        sort(so.begin(),so.end(),cmp);\n        for(int i=0;i<num;i++){\n            ans.emplace_back(so[i].ver);\n        }\n        return ans;\n    }\n};","compare_result":"11111000000000000000000000000000000000000000","title_slug":"course-schedule-ii"}